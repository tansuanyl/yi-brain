# 易脑 Yi-Brain 项目说明

## 项目定位

Yi-Brain 是一个以《易经》结构为底层推演框架、以大模型自然语言能力为输出界面的问答解惑应用。

它不是把易经当成“神秘答案生成器”，而是把易理中的：

- 起卦
- 变爻
- 时位
- 本卦与之卦转换

转译成一套可解释、可复用、可产品化的推理工作流。

## 核心方法论

### 1. 占卜式 Prompting

用户输入问题后，系统根据问题文本和上下文生成确定性种子，完成一次“起卦”。

这个过程在产品里承担三件事：

- 为同一问题生成稳定但非机械的结构化视角
- 用随机性打破模型或用户的思维定势
- 让问答从“直接给答案”转成“先建模，再建议”

### 2. 变爻推理

系统先得到本卦，再依据变爻生成之卦。

本卦用于描述当前局势，之卦用于描述演化方向。两者的对照可以天然转成：

- 如果继续保持当前路径，会怎样
- 如果主动调整策略，会怎样
- 不同选项分别在什么阶段更匹配局势

### 3. 时位感知解码

六爻被解释为六个阶段：

1. 初始探索
2. 进入互动
3. 中段校准
4. 外部显化
5. 接近结果
6. 终局定案

在产品设计上，这对应：

- 前段更开放的探索建议
- 中段更注重校准关系与资源
- 后段更强调收敛和决断

## 当前仓库内容

- `public/`: 前端界面
- `server/index.js`: Express 入口
- `server/services/intent.js`: 意图识别
- `server/services/iching.js`: 易理推演引擎
- `server/services/llm.js`: OpenAI 兼容模型调用
- `render.yaml` / `railway.json` / `Dockerfile`: 部署文件

## 当前版本能力

- 支持 Web 端输入问题、背景、分类与方案选项
- 支持自动识别问题类别与情绪状态
- 支持起卦、本卦、变卦、变爻、阶段策略输出
- 支持规则式解释
- 支持接 OpenAI 兼容接口生成现代语言解读

## 下一阶段建议

- 补充更完整的 64 卦知识库与 384 爻辞语义层
- 增加用户历史问事记录
- 增加“多轮追问”能力
- 增加会员体系与分享海报
- 增加小程序端和 API 商品化能力
